从你这张图看，**问题大概率不只是“矿物表面电导参数调小/调大”这么简单**。现在的模拟结果和论文数据差了两个层面：

1. **实部 (\sigma')**：论文数据大约是 (0.1\sim0.4\ \mathrm{S/m})，你的 AC2D 只有 (0.007\sim0.013\ \mathrm{S/m})。
2. **虚部 (\sigma'')**：论文数据大约是 (10^{-2}\ \mathrm{S/m}) 量级，你的 AC2D 是 (10^{-8}\ \mathrm{S/m}) 量级。

所以我判断：**实部主要是水相/酸液电导率或几何换算问题；虚部主要是 EDL 极化强度/表面导纳映射问题。**

---

## 1. 实部差这么多，优先检查 (\sigma_w(t))

Rembert 实验里水相电导率不是一直 (0.01\ \mathrm{S/m})。

初始平衡溶液：

[
\sigma_{S0}=0.01\ \mathrm{S/m}
]

注入 HCl 溶液：

[
\sigma_{\mathrm{HCl}}=0.44\ \mathrm{S/m}
]

所以实验实部前期升到 (0.3\sim0.4\ \mathrm{S/m}) 是合理的：它主要反映酸液进入、电解质增强、方解石溶解产生离子。

你现在 AC2D 的实部曲线只有 (0.01\ \mathrm{S/m}) 附近，这很像是：

[
\sigma_w(t)\approx 0.01\ \mathrm{S/m}
]

一直没有真正被更新到 HCl/溶解后的水相电导率。

所以第一件事不是调表面电导，而是检查你的代码里是不是仍然用了：

[
\sigma_w = 0.01\ \mathrm{S/m}
]

或者把 PHREEQC / 数据集里的 (\sigma_w(t)) 单位读错了。

如果你要复现 Rembert 的实部，水相电导率应该至少能从 (0.01\ \mathrm{S/m}) 上升到接近 (0.4\ \mathrm{S/m}) 的量级。否则无论你怎么加颗粒极化，实部都不会到论文曲线的量级。

---

## 2. 你的虚部差 (10^6) 倍，说明 EDL 极化几乎没有生效

你的 (\sigma'') 是 (10^{-8}\ \mathrm{S/m})，而论文是 (10^{-2}\ \mathrm{S/m}) 量级。这种差距通常不是普通参数误差，而是模型映射出了问题。

如果你用了 Niu 2020 里类似 Schwarz 模型的形式：

[
C_p^* =
\frac{i\omega \tau_p}{1+i\omega \tau_p}\Sigma_S
]

然后再转成水相附加复电导率：

[
\Delta \sigma_w^*=\frac{2C^*}{\Lambda}
]

那你要特别小心：**Niu 论文里的表面电导参数是为砂岩/硅质表面设置的，不适合直接搬到 Rembert 的方解石溶解实验里。**

Niu 里常用的 (\Sigma_S) 是 (10^{-9}\ \mathrm{S}) 量级。如果你直接用这个值，估算一下：

[
\Delta \sigma \sim \frac{2\Sigma_S}{\Lambda}
]

假设特征长度：

[
\Lambda \sim 10^{-4} \sim 10^{-3}\ \mathrm{m}
]

那么：

[
\Delta \sigma \sim 10^{-6}\sim10^{-5}\ \mathrm{S/m}
]

这本来就远低于 Rembert 的：

[
\sigma'' \sim 10^{-2}\ \mathrm{S/m}
]

所以你的虚部小很多并不意外。**Rembert 的极化强度不是靠固定的砂岩表面电导来给的，而是通过 CEC / (Q_v) 反演出来的等效表面极化能力。**

---

## 3. 你现在可能把 Rembert 的 CEC 物理量和 Niu 的 (\Sigma_S) 没有正确对应起来

Rembert 的等效电路模型是：

[
\sigma''=\phi^m S_w^{n-1}\lambda Q_v
]

[
Q_v=\frac{1-\phi}{\phi}\rho CEC
]

它不是直接输入 (\Sigma_S)，而是通过：

[
CEC \rightarrow Q_v \rightarrow \sigma''
]

来控制极化强度。

而 Niu 的孔隙尺度框架里，EDL 极化通常是通过：

[
\Sigma_S,\ D,\ r,\ \tau
]

这类参数进入。

所以你如果想用 Niu 框架模拟 Rembert，必须建立一个映射关系：

[
CEC(t)\quad \Longrightarrow \quad \Sigma_S(t)
]

或者：

[
CEC(t)\quad \Longrightarrow \quad \text{surface admittance } Y_s^*(t)
]

不能直接把 Niu 的默认 (\Sigma_S=10^{-9}\ \mathrm{S}) 拿来用。

---

## 4. 还要重点检查 CEC 单位换算

这个地方非常容易错。

Rembert 里面 CEC 通常以：

[
\mathrm{mEq/g}
]

或者类似单位表示。但在公式：

[
Q_v=\frac{1-\phi}{\phi}\rho CEC
]

里，(Q_v) 是电荷密度，应该是：

[
\mathrm{C/m^3}
]

所以 CEC 需要转换成：

[
\mathrm{C/kg}
]

换算关系是：

[
1\ \mathrm{meq/g}
=================

96.485\ \mathrm{C/kg}
]

如果你把：

[
1\ \mathrm{meq/g}
]

错误地当成：

[
10^{-3}
]

或者没有乘 Faraday 常数 (F=96485\ \mathrm{C/mol})，那么 (Q_v) 会小 (10^5) 量级，虚部 (\sigma'') 也会跟着小 (10^5) 量级。

这和你图里虚部差 (10^6) 倍非常接近。

所以我强烈建议你检查：

[
CEC\ [\mathrm{mEq/g}]
\rightarrow CEC\ [\mathrm{C/kg}]
]

有没有正确转换。

---

## 5. 还有一个关键问题：Rembert 的数据不是普通体积平均电导率

Rembert 的 SIP 实测电导率是通过：

[
\sigma^*=\frac{1}{k_G Z^*}
]

得到的，其中 (k_G) 是微流控芯片电极几何因子。

你的 AC2D 如果输出的是：

[
\sigma_{\mathrm{eff}}^*
=======================

\frac{\langle J\rangle}{\langle E\rangle}
]

那它和 Rembert 的实验处理方式不一定完全一致。

尤其是微流控芯片是：

[
L \times w \times h
===================

4000\ \mu m \times 1500\ \mu m \times 150\ \mu m
]

如果你的 2D 模型默认单位厚度，或者没有正确乘以 (h=150\ \mu m)，那么阻抗到电导率的换算会偏很多。

所以你需要确认你比较的是同一个量：

[
\text{AC2D 输出}
\quad \text{vs}
\quad
\frac{1}{k_G Z^*}
]

而不是直接把内部平均电导率拿去和论文的 apparent/effective conductivity 比。

---

## 6. 我建议你的排查顺序

按优先级来：

### 第一，先关掉极化，只检查实部

只模拟纯导电：

[
\sigma_w(t)
]

不加 EDL，不加界面极化。看 AC2D 的 (\sigma') 能不能达到论文实部的量级。

如果还是只有 (0.01\ \mathrm{S/m})，说明问题在：

[
\sigma_w(t)
]

或者几何换算。

### 第二，用 Rembert 的 Waxman–Smits 公式单独复算一遍

用同一组：

[
\phi(t),\quad S_w(t),\quad \sigma_w(t),\quad CEC(t)
]

先算：

[
\sigma'*{\mathrm{WS}}(t),\quad \sigma''*{\mathrm{WS}}(t)
]

如果 Waxman–Smits 能接近论文，而 AC2D 不行，问题就在你把 CEC/EDL 极化映射到 Niu 框架的过程。

### 第三，再调 (\Sigma_S) 或表面导纳

不要用 Niu 的砂岩默认值。你需要让它满足同量级：

[
\Delta \sigma''_{\mathrm{EDL}}
\sim
10^{-2}\ \mathrm{S/m}
]

而不是 (10^{-8}\ \mathrm{S/m})。

---

## 7. 结论

你的结果差，**不是简单说明“界面极化 + 颗粒极化这个思路错了”**。更可能是这几个问题：

[
\boxed{\sigma_w(t)\ \text{没有正确使用 HCl/地球化学模拟电导率}}
]

[
\boxed{CEC \rightarrow Q_v \rightarrow \Sigma_S/Y_s^*\ \text{映射不对}}
]

[
\boxed{CEC 单位没有从 mEq/g 正确转成 C/kg}}
]

[
\boxed{2D/3D 几何因子和芯片厚度换算不一致}}
]

从图上看，**实部先查水相电导率；虚部先查 CEC 单位和表面极化强度映射**。这两个比“微调矿物表面电导”更关键。
