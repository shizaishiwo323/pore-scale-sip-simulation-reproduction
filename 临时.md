下面把路线明确成一个**project-extracted 复现计划**：目标不是“严格复原 Niu 作者的隐藏孔网”，而是用**本项目自己从 Niu Berea microCT 提取的孔网**完成一条闭合、可追溯、可投稿说明的复现链。

依据上，Niu 2020 的核心框架是用数字微结构计算宽频有效电导率/介电常数，并同时考虑 interfacial 与 electrochemical polarizations；主文明确用 Berea 样品和 (10^{-3}) 到 (10^{9}) Hz 实验数据验证模拟。 SI 也说明真实微结构路线的优势是能同时保留水相电连通性和固液界面电学不连续性，这正好支持我们坚持 project-extracted microCT→PNM→AC3D 的路线。

## 0. 路线命名先固定

正式命名建议统一为：

```text
Niu2020 Berea project-extracted pore-network reproduction
```

中文说明：

```text
基于本项目从 Niu 2020 Berea microCT 重新提取孔隙网络的 Niu 框架复现
```

不要叫：

```text
strict Niu2020 reproduction
paper-original membrane reproduction
```

因为 Figure 5 工作簿只给孔径/孔喉分布，不给每条喉道的半径、截面积和 (Z_{dc})，所以 membrane 机制必须使用本项目 pnextract 网络几何来闭合。你已有文档也写过：Figure 5 路线膜极化列为空，完整膜极化需要 pnextract 几何近似；这条路线应解释为自动提取网络路线，而不是论文作者原始网络。

---

# 总体计划安排

## 阶段 1：先把 project-extracted 的“红线”写进代码和结果包

目标：保证后续所有结果不会被误解为论文隐藏参数复现。

必须固定 5 条红线：

1. **不使用 Figure8.xlsx 的 Simulation / Pore / Membrane / Interfacial 列作为本项目模拟结果。** 只允许用实验列做对比，paper simulation 只能做参考审计。你现有 verifier 已经有这个方向。
2. **不做 post-extraction geometry scaling。** 不允许对 pnextract 后的孔径、孔喉长度、(Z_{dc}) 加缩放因子；项目配置里已经写明该政策。
3. **四机制必须按 Section 5.3 独立跑场解。** interfacial / pore / membrane / all 的材料赋值定义已经在配置中写对了。
4. **all 不能由有效电导率代数拼接。** 必须从水相属性层组合 pore+membrane，再重新跑 full-grid AC3D。
5. **所有正式图必须追溯到 `results/<run>/simulation_sweeps/<mechanism>/<direction>/sweep_results.csv`。** AGENTS 里已经规定正式 sweep 必须放在结果包内部并保留每频点结果和收敛信息。

交付物：

```text
docs/plans/niu2020_project_extracted_reproduction_plan.md
configs/sip_simulation/niu2020_berea_project_extracted_v1.py
```

---

## 阶段 2：建立闭合的 microCT → pnextract → network manifest

目标：本项目提取的孔网必须成为唯一几何来源。

输入固定：

```text
data/Niu 2020data/microCT_Berea.raw
data/Niu 2020data/microCT_Berea.tiff
shape = 350 × 350 × 350
voxel size = 2.8 um
label 1 = pore/water
label 2 = solid
```

这些值在当前项目说明中已经作为 Niu Berea formal 路线约定。

需要做的代码改动：

1. 新增 `code/scripts/data_audit/validate_niu2020_berea_volume.py`

   * 检查 RAW 文件大小；
   * 检查标签只含 1/2；
   * 输出 label count、孔隙率、连通孔隙率；
   * 输出 RAW canonical hash。
2. 修改 `build_niu2020_berea_result_package.py`

   * 不再用“多数类=solid”的自动推断作为 formal 模式；
   * formal 模式必须显式 `1 -> pore, 2 -> solid`。
3. 新增 `code/scripts/pore_network/build_project_extracted_network.py`

   * 统一调用 pnextract；
   * 解析 `pores.csv`、`throats.csv`；
   * 输出 network hash、pnextract 参数、pnextract 二进制 hash；
   * 记录 boundary throat 是否剔除。

验收标准：

```text
results/niu2020_berea_project_extracted_v1/
  geometry/
    volume_audit.json
    label_stats.csv
  pore_network/
    pnextract_inputs/
    network_parsed/pores.csv
    network_parsed/throats.csv
    network_manifest.json
```

注意：Niu SI 说明 network model 可以显式定义 pore size 和 pore throat size/distribution，而且现实孔网可从 μCT 数字微结构提取；这就是 project-extracted 路线的理论合法性。

---

## 阶段 3：把 project-extracted 几何转成极化频谱

目标：用**自己提取的 pores/throats**计算 pore 和 membrane increments。

### 3.1 pore polarization

使用 `pores.csv`：

```text
radius = pore_radius_m
weight = pore_volume_m3
tau_p = r^2 / (2D)
C_p* = Sigma_S * iωtau_p / (1 + iωtau_p)
Delta sigma_pore* = 2 C_p* / Lambda
```

你当前 `polarization.py` 中 pore 公式实现基本符合这个目标。

### 3.2 membrane polarization

使用 `throats.csv`：

```text
L = throat_length_m 或明确记录的 membrane_relaxation_length_m
A = throat_radius_m^2 / (4 * throat_shape_factor)
Zdc = L_zdc / (sigma_w * A)
tau_m = L^2 / (4D)
C_m* = 1 / Z_m* - 1 / Zdc
Delta sigma_membrane* = 2 C_m* / Lambda
```

当前代码已经支持从 throat geometry 反算 area 和 (Z_{dc})。 但必须把它明确标为：

```text
Zdc source = project-extracted pnextract throat geometry
```

不能写成 Niu paper-provided (Z_{dc})。

交付物：

```text
polarization_spectra/
  project_extracted_base_spectrum.csv
  project_extracted_base_spectrum_manifest.json
  components_paper_mode/
    polarization_spectra_interfacial.csv
    polarization_spectra_pore.csv
    polarization_spectra_membrane.csv
    polarization_spectra_all.csv
    metadata.json
```

机制谱生成时继续采用当前 paper mode：pore-only 和 membrane-only 不加入水/固相介电背景；interfacial 和 all 才保留高频介电项。现有 `make_polarization_component_spectra.py` 已按这个定义实现。

---

## 阶段 4：先修 AC3D 正式可信度，再跑 full-grid

这是 project-extracted 复现能否成立的关键。

### 必修 4.1：zero-solid pore/membrane-only 的零空间处理

pore-only 和 membrane-only 固相为 0，这会让固体区域产生大量零空间。必须在正式 project-extracted 版本中处理：

```text
方案 A：active pore-domain solver
方案 B：每个连通分量 gauge fixing / nullspace projection
```

建议先做 A：只在导电水相连通域上求解 pore/membrane-only，固相不进入未知量。

### 必修 4.2：GPU true residual

当前 GPU BiCGSTAB 返回的是递推残差，正式结果要增加：

```text
recursive_relative_residual
true_relative_residual = ||b - A x|| / ||b||
effective_sigma_change_check
```

NISTIR 6269 明确 AC3D.F 是 finite difference AC conductivity 程序，且手册列出了 AC3D.F 作为关键程序；所以我们至少要把当前 GPU 解算结果与 NIST/AC3D 思路的边界、残差和有效性质计算口径对齐。

### 必修 4.3：方向平均

正式 project-extracted 不只跑 x 方向。必须跑：

```text
x, y, z
```

输出：

```text
sigma_eff_x
sigma_eff_y
sigma_eff_z
directional_mean
anisotropy_ratio = max/min
```

交付物：

```text
code/src/pore_scale_electrical/ac3d_active_domain.py
code/src/pore_scale_electrical/residual_checks.py
code/tests/test_zero_solid_pore_only.py
code/tests/test_true_residual_gpu.py
```

---

## 阶段 5：分三档跑，避免一开始 full-grid 卡死

### 5.1 smoke

目的：检查流程，不看物理结论。

```text
shape: 32^3 或 50^3 crop
frequencies: 1e-3, 1, 1e3, 1e6, 1e9
mechanisms: interfacial, pore, membrane, all
directions: x
dtype: complex64
```

输出目录：

```text
results/niu2020_berea_project_extracted_v1_smoke/
```

### 5.2 diagnostic

目的：检查峰频、数值稳定性和三方向差异。

```text
shape: 350^3
frequencies: 每 decade 至少 3–5 点
directions: x, y, z
dtype: complex64
selected checkpoint: complex128
```

输出目录：

```text
results/niu2020_berea_project_extracted_v1_diagnostic/
```

### 5.3 formal project-extracted

目的：作为论文/报告主结果。

```text
shape: 350^3
downsample: 1
crop: None
frequencies: 10^-3 到 10^9 Hz，建议 97 点或至少与论文 Figure 数据统一的 40 点
mechanisms: interfacial / pore / membrane / all
directions: x / y / z
dtype: complex64 + selected complex128 audit
preconditioner: fft
rtol: 1e-5 起步，低频 interfacial/all 做更严格复核
```

输出目录：

```text
results/niu2020_berea_project_extracted_v1_formal/
```

---

## 阶段 6：图件与 provenance 只画 project-extracted

正式图只包含：

```text
Experiment
Project-extracted interfacial
Project-extracted pore
Project-extracted membrane
Project-extracted all
```

不要再混入：

```text
Paper simulation
Best-EDL diagnostic
Algebraic diagnostic all
Legacy scaled membrane
```

论文 simulation/component 列可以在附录或 provenance 表中作为：

```text
paper_reference_not_used_as_project_result
```

图件 source data 必须包含：

```text
dataset
mechanism
frequency_hz
sigma_real_s_m
sigma_imag_s_m
direction
direction_aggregation
source_sweep_csv
source_manifest
paper_simulation_columns_used = False
run_mode = formal_project_extracted
```

---

## 阶段 7：最终验收指标

project-extracted 复现最终至少要给出这几张表：

### 表 1：几何审计

```text
voxel_porosity
connected_porosity_x/y/z
surface_area_estimate
formation_factor_x/y/z
pore_count
throat_count
pore_radius_mean_volume_weighted
throat_length_mean_volume_weighted
distance_to_Niu_Figure5_pore_distribution
distance_to_Niu_Figure5_throat_distribution
```

孔喉长度非常关键，因为低频 SIP 弛豫和孔喉尺度关系在文献中有实验支持；Scott & Barker 说明砂岩低频相位峰与 dominant pore-throat size 相关，且认为 pore-throats 和孔几何对低频同相/异相电导都不可忽略。

### 表 2：极化输入审计

```text
Sigma_S
D
Lambda
eta0
sigma_w
epsilon_w
epsilon_s
pore_weight_source
membrane_weight_source
Zdc_source
boundary_throats_included
```

### 表 3：求解器审计

```text
mechanism
direction
frequency_count
max_recursive_residual
max_true_residual
max_iterations
failed_frequency_count
complex128_checkpoint_error
```

### 表 4：结果对比

```text
mechanism
peak_frequency_sigma_imag
peak_sigma_imag
ratio_to_experiment_peak
RMSE_log_sigma_real
RMSE_log_sigma_imag
directional_anisotropy
```

---

# 建议时间安排

## 第 1 周：把 project-extracted 结果包骨架做出来

重点是**闭合 provenance**，不是跑大模拟。

任务：

```text
1. 写 niu2020_berea_project_extracted_v1.py 配置
2. 写 volume audit
3. 写 project-extracted pnextract builder
4. 写 network manifest
5. 写 base spectrum + component spectra manifest
```

验收：

```text
不用 GPU，也能生成完整 geometry/network/polarization_spectra 目录。
```

## 第 2 周：修 AC3D 可信度

任务：

```text
1. zero-solid active-domain solver
2. GPU true residual
3. formal/smoke/diagnostic run_mode fail-closed
4. 禁止 nearest frequency 静默匹配
5. 禁止 algebraic all
```

验收：

```text
小网格 zero-solid pore/membrane-only 测试通过；
true residual 与 recursive residual 同时输出；
正式绘图不能接受 smoke CSV。
```

## 第 3 周：跑 smoke + diagnostic

任务：

```text
1. smoke 5 频点跑通四机制
2. diagnostic full350 低密度频率网格跑通 x/y/z
3. 选 3–5 个关键频点做 complex128 checkpoint
4. 输出初版 Figure 7/8 style project-extracted 图
```

验收：

```text
所有曲线都能追溯到 project-extracted sweep_results.csv；
没有 paper simulation 列；
没有 abs() 掩盖符号。
```

## 第 4 周：formal project-extracted

任务：

```text
1. full350 × 3方向 × 4机制 × 正式频率网格
2. 生成 source_data/provenance/manifest
3. 写结果讨论：几何偏差如何影响 membrane 峰位和幅值
4. 与 Niu 实验和 paper-reference simulation 分开对比
```

验收：

```text
results/niu2020_berea_project_extracted_v1_formal/
```

可以作为论文或报告中的主结果目录。

---

# 最终口径建议

你后续论文/报告中可以这样写：

> 本研究没有使用 Niu et al. (2020) 工作簿中的 simulation/component 数值作为本项目模拟结果，而是从公开 Berea microCT 分割体重新提取孔隙网络，并使用该 project-extracted network 计算 pore 和 membrane 极化输入，再在 350³ 体素 AC3D 网格上分别求解 interfacial、pore、membrane 和 all mechanisms。由于 Niu 2020 未公开每条孔喉的 (Z_{dc}) 与完整网络拓扑，本路线应解释为 Niu 框架下的 project-extracted reproduction，而非作者原始 PNM 的逐项复原。

这个口径科学上最稳，也最符合你现在项目的真实方向。
