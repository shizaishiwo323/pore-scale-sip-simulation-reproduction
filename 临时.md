结论：**核心 pnextract 仍然符合 Dong & Blunt 2009 这篇论文的 maximal-ball / pore-throat chain / pore-space segmentation 思路；但当前 v2 不是“论文原版 pnextract 的原样导出结果”。** 它是在原始 pnextract 已经生成聚合 throat 之后，对 `link1/link2` 导出做了一个面向 SIP 膜极化的二次电学几何修正。

**和原始 `code/vendor/pnextract` 的区别**
只改了复制版源码里的一个位置：

[blockNet_write_cnm.cpp](C:/Users/imgw/Documents/Codex/SIP模拟/sip模拟/results/niu2020_berea_contact_split_conserve_pnextract_build_v2/contact_patch_split/source/src/pnm/pnextract/blockNet_write_cnm.cpp:146)

没有改原始 vendor：

`git diff -- code/vendor/pnextract` 是空的。

原始版本在 [blockNet.cpp](C:/Users/imgw/Documents/Codex/SIP模拟/sip模拟/code/vendor/pnextract/src/pnm/pnextract/blockNet.cpp:330) 的 `createNewThroats()` 中，用 `pore pair -> throat id` 的 `contacts` map 聚合 throat。也就是说，同一对 pore 之间多个接触面会累加到同一个 `throatNE` 里。

原始导出在 [blockNet_write_cnm.cpp](C:/Users/imgw/Documents/Codex/SIP模拟/sip模拟/code/vendor/pnextract/src/pnm/pnextract/blockNet_write_cnm.cpp:149) 直接按 `throatIs.size()` 写一条 throat。长度定义是：

[blockNet_write_cnm.cpp](C:/Users/imgw/Documents/Codex/SIP模拟/sip模拟/code/vendor/pnextract/src/pnm/pnextract/blockNet_write_cnm.cpp:77)

```cpp
lp1 = lpt1 * 0.67;
lp2 = lpt2 * 0.67;
lthroat = lengthP1toP2 - lp1 - lp2;
if (lthroat < 0.0000001) lthroat = 1;
```

当前 v2 复制版则在导出阶段做了这些事：

- 收集每条聚合 throat 的 `toxels1 + toxels2` 接触面体素。
- 按接触面体素的空间连通分量拆成多个 `ContactSplitRecord`。
- 设置 `minThroatLengthVoxels = 0.25`，避免 v1 那种 near-zero throat。
- 按原始 throat 的 `A/L` 做电导守恒分摊：`originalAreaOverLength * faceCount / totalFaces`。
- 由目标面积和 shape factor 回算 split throat radius。
- `volume` 仍按接触面数量比例分配。
- 额外写出 `_contact_patch_split_diagnostics.csv`。

关键代码在：

[build_patched_pnextract_contact_split.py](C:/Users/imgw/Documents/Codex/SIP模拟/sip模拟/code/scripts/pore_network/build_patched_pnextract_contact_split.py:95)

**和论文是否一致**
分层看：

**1. 最大球、孔体识别、孔喉链构建：一致。**  
论文的核心是 modified maximal ball algorithm：最大内切球、去冗余、按大小和层级聚类，形成 pore-throat chains。我们没有改这些部分。

**2. “多条 parallel links 后续 merge”：当前 v2 不完全一致。**  
论文明确说，两个 family trees 之间可能有一条或多条 pore-throat chains，**这些 parallel links 后续会 merge**。而我们的 v2 正是在这个 merge/聚合之后，根据接触面连通性把一条 exported throat 再拆成多条。  
所以从“论文原始最终网络拓扑”的角度：**这是偏离**。

**3. 从论文方法精神看：可以辩护为合理扩展。**  
论文也明确说 pore/throat 的划分有一定任意性，而且会影响 transport properties，需要通过参考数据优化。我们的修正正是因为 SIP 膜极化对 throat `L` 和 `Zdc` 极其敏感，原始 merged throat 会丢失短喉电学信息。  
因此它符合论文“基于几何抽取、再通过输运性质校准网络参数”的思想，但不是论文原版算法的严格实现。

**4. 当前 v2 仍有一个需要谨慎表述的点。**  
代码里 `canSplitInternal = tr.e1 >= 2 && tr.e2 >= 2 && patchVoxels.size() > 1`，所以只要是内部 throat 且接触面体素超过 1，就会进入重算逻辑；即使最后只有一个连通分量，也会用 centroid 和 conductance-conserving 规则重算导出几何。  
如果你想更严格地贴近论文原始导出，可以改成：**只有 `patchCount > 1` 时才拆分/重算；`patchCount == 1` 时保留原始 `t_ltrot/t_shapeFacts/t_radiuss`。**

**一句话判断**
当前 v2 pnextract 是：

> Dong & Blunt maximal-ball pnextract 的“核心算法不变 + SIP 膜极化专用 throat 导出修正版”。

它**仍符合论文的总体理论和抽取思路**，但**不应称为论文原版 pnextract 输出**；更准确的名字应是：

`contact-split conductance-conserving pnextract export for SIP membrane diagnostics`。